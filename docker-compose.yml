db:
  image: mongo
  expose:
    - "27017"
    - "37017"
  command: --smallfiles
web:
  build: .
  ports:
    - "8080:8080"
  env_file:
    - meanbase.env
  links:
      - db:db
nginx:
  # restart: always
  build: nginx
  links:
    - web:web
  volumes_from:
    - web
  ports:
    - "80:80"
