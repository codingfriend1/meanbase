angular.module('meanbaseApp')
  .controller('pageTemplateCtrl', function ($scope, endpoints, $rootScope, $timeout, api) {
  	// Everything needs to be wrapped in a timeout
  	$timeout(function() {

  	});
  });
